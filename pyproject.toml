[project]
name = "mlflow-39-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.42.52",
    "mlflow",
]

[tool.uv.sources]
mlflow = { path = "../mlflow" }
